# Frontend environment — these are PUBLIC and shipped to the browser.
# Never place real secrets here. The backend owns all credentials.

# Base path of the backend API. In dev this is proxied to the Express server.
VITE_API_BASE_URL=/api
